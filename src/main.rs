use net::*;
// use net::connection::*;
use net::rot_net::*;
use net::*;
use std::cell::{Cell, RefCell};
use std::{boxed::Box, ops::DerefMut};

fn main() {
    println!("Hello, world!");
    // unittests per feature from here on out. initial topology is setup and forward propagation is outlined
    // let subject = rot_net{inputs: ,outputs: ,};
    // let init_nodes = network::initialize_network(3, 2);
    // let subject =
    //     network::rot_net_initialize(init_nodes.0.iter().collect(), init_nodes.1.iter().collect());

    // let a = subject.cycle(vec![1, 2, 3]);
    // a.into_iter().for_each(|x| println!("{}", x));
}
